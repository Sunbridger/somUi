import FlexboxItem from '../flexbox/src/flexbox-item';

/* istanbul ignore next */
FlexboxItem.install = function(Vue) {
    Vue.component(FlexboxItem.name, FlexboxItem);
};

export default FlexboxItem;
